> Status: NeedsReview  
> 이 문서는 권위, 자동 생성 경로, 또는 현재 기준 여부 확인이 필요합니다. 현재 기준은 `docs/DOCUMENT_REGISTER.md`를 따릅니다.

# Contract Enforcement Status

This document records how far the current `Contract_Enforcement_Matrix.md` contracts are connected to executable gates.
It does not add new contracts and does not change the meaning of the CEM rows.

Status values:

- `CONNECTED`: enforced by an existing gate or validator and covered by self-test or regression.
- `PARTIAL`: some enforcement exists, but one or more runtime/evidence/self-test links still need tighter coverage.
- `TODO`: not yet connected beyond documentation or manual review.

| CEM ID | Contract name | Static gate status | Runtime validation status | Evidence status | Self-test status | Remaining work |
|---|---|---|---|---|---|---|
| CEM-001 | Runtime mutation is transactional | CONNECTED: `check_apply_guard_transaction_patterns`, `check_apply_guard_rollback_failure_ownership` | PARTIAL: rollback failure and preserved-state outcomes are detected through runtime/evidence logs; branch-level control-flow proof remains manual | CONNECTED: `apply_guard_rollback_failure_count`, `rollback_failure_transferred_to_shutdown_count`, `rollback_preserved_state_count`, `blocker_count` | CONNECTED: ApplyGuard rollback failure, missing transfer, transfer-present, destructor failure, unsafe discard fixtures | TODO: keep branch-level rollback/discard path review manual until a control-flow-aware checker exists |
| CEM-002 | ThreadTracker is observation-only | CONNECTED: `check_thread_tracker_runtime_contracts` and forbidden mutation API scan | PARTIAL: ThreadTracker telemetry records access/query/reset events without mutation | PARTIAL: `thread_tracker_telemetry` is parsed and exposed, but access/query failure fixture density is limited | PARTIAL: regression covers telemetry summary and observation-only static markers | TODO: add direct evidence fixture for OpenThread/GetThreadTimes failure telemetry if telemetry schema changes |
| CEM-003 | Thread scheduling mutation owner | CONNECTED: `check_apply_guard_transaction_patterns`, B2 expected scanner, SchedulerController helper markers | CONNECTED: post-apply and post-failure affinity/priority audits are required before commit, rollback, or audited discard | PARTIAL: `test_results`, `blocker_count`, affinity audit failure markers, and no-side-effect discard evidence are connected | CONNECTED: affinity audit query failure, audit mismatch, no-side-effect discard, and AntiCheatFallback helper marker tests | TODO: keep manual review for future thread mutation entry points outside current SchedulerController path |
| CEM-004 | Processor group policy | CONNECTED: `check_background_processor_group_policy_is_explicit` | CONNECTED: group 1+ process-wide background restriction is blocked and logged as monitoring-only | CONNECTED: `processor_group_mode`, `background_restriction_mode`, `warn_count` | CONNECTED: group 1+ mock and ProcessorGroup/HEDT evidence tests | TODO: future priority-only group 1+ work requires a new contract before enabling mutation |
| CEM-005 | SoftApply is evidence, not rollback state | CONNECTED: `check_release_evidence_contract`, `check_apply_mode_policy_contract`, SoftApply blocker marker checks | CONNECTED: SoftApply baseline is audit evidence and must not become preserved rollback state | CONNECTED: `soft_apply_baseline_summary`, `rollback_preserved_state_count` | CONNECTED: SoftApply baseline summary fixture proves preserved rollback count remains `0` | TODO: maintain separation if a dedicated SoftApply baseline store is introduced later |
| CEM-006 | Release decisions are evidence-driven | PARTIAL: `check_release_evidence_contract`, `check_rc_candidate_contract`, 9-step RC order gate, and standalone soak wrapper gate are connected | PARTIAL: release smoke, evidence bundle generation, and `verify-rc` are in the RC gate; 30m dry-run and 60m soft-apply soak have standalone entry points | PARTIAL: schema hash, commit, binary SHA-256, blocker/warn/info counts, soak timeline/heartbeat checks, test results, and final bundle manifests are connected | CONNECTED: release evidence self-test, static gate self-test, standalone soak wrapper selftest, candidate and bundle validator selftests | TODO: run 30m dry-run soak, 60m soft-apply soak, and real-game validation before tagging |
| CEM-007 | Background restriction owner | CONNECTED: `check_apply_mode_policy_contract`, `check_apply_guard_transaction_patterns`, protected process checks | PARTIAL: process apply failure audit and access-boundary fallback behavior are logged; full runtime injection matrix is limited | PARTIAL: `background_restriction_mode`, `access_denied_fallback_summary`, `blocker_count` are exposed | PARTIAL: BackgroundController safety tests and access-denied fallback evidence selftest cover primary paths | TODO: add dedicated process no-side-effect discard fixture if process restriction failure handling expands |
| CEM-008 | Access boundary fallback | CONNECTED: `check_anti_cheat_fallback_contract` | CONNECTED: denied access remains monitoring-only or requires rollback/fallback evidence | CONNECTED: `access_denied_fallback_summary`, `warn_count`, `blocker_count` | CONNECTED: Access Denied fallback WARN-only fixture and anti-cheat fallback tests | TODO: keep Apply mode blocked for new anti-cheat scenarios until fallback evidence is present |
