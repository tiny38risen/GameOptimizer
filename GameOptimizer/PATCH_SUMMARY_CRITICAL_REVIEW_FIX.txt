PATCH SUMMARY — Critical Review Fix

1. SchedulerController expected access
   - validateSchedulingRightsAndState and applyMainThreadPolicy already use Assign -> Check -> Bind.
   - No std::expected::operator-> value access remains in SchedulerController.

2. ThreadTracker expected access
   - observeOnce now binds queryThreadCpuTime100ns result before value use:
     auto currentCpuTime = ...; if (!currentCpuTime) ...; const std::uint64_t& cpuTime100ns = *currentCpuTime;

3. ThreadTracker hot-path exception boundary
   - Removed try/catch from observeOnce and update.
   - Runtime sampling path now returns explicit std::expected errors only.

4. ThreadSample reset ownership ordering
   - ThreadSample::reset releases WinHandle before clearing threadId and scalar state.

5. ThreadTracker fixed-capacity lookup
   - Replaced repeated full-array find with fixed-capacity open-addressed hash lookup.
   - Added active slot list and free slot stack.
   - deactivateMissingSamples, resetMultiSampleAccumulators, finalizeMultiSample, updateCandidateState iterate active slots only.

6. Config normalization
   - Constructor normalizes sampleCount, emaAlpha, stickinessDuration, and negative sampleInterval.

7. main.cpp expected access
   - TopologyAnalyzer result now binds to const reference before member access.
   - gRunning release/acquire pairing remains explicit.

2026-05-07 follow-up merge-blocker fix
- main.cpp: replaced post-check direct expected dereference use with explicit const reference binding before consumption.
- ThreadTracker: added fixed-storage invariant failure reset path.
- ThreadTracker: releaseSampleSlot now validates activePositions_ and activeSlots_ consistency before freeing a slot.
- ThreadTracker: eraseSlotIndex now clears stale hashThreadIds_ metadata during deletion and rehash.
- ThreadTracker: update now logs ThreadTracker observation context before returning ErrorCode.
