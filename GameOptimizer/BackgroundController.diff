--- BackgroundController.cpp
+++ BackgroundController.cpp
@@
         const DWORD currentPriority = GetPriorityClass(processHandle.get());
         if (currentPriority == 0)
         {
             ++summary.skippedProcessCount;
             Logger::warn(
                 "background process skipped: {} (pid={}, reason=priority_query_failed)",
                 processNameForLog,
                 processId);
             continue;
         }
 
+        if (processMask == targetMask && currentPriority == IDLE_PRIORITY_CLASS)
+        {
+            ++summary.skippedProcessCount;
+            Logger::info(
+                "background process skipped: {} (pid={}, reason=already_restricted, affinity=0x{:X}, priority=IDLE)",
+                processNameForLog,
+                processId,
+                static_cast<unsigned long long>(processMask));
+            continue;
+        }
+
         const auto saveResult = rollbackManager_.saveProcessState(processId, processMask, currentPriority);
