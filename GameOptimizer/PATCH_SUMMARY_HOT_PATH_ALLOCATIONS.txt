[PATCH] Hot-path allocation hardening

Changed files:
- LatencyDecisionLayer.h
- LatencyDecisionLayer.cpp
- LatencyDecisionLayerTests.cpp
- AppliedPolicyTracker.h
- AppliedPolicyTracker.cpp
- AppliedPolicyTrackerTests.cpp
- LatencyMetricsCollector.cpp
- main.cpp

Fixes:
1. Replaced LatencyDecisionLayer std::vector<PolicyCommand> return with PolicyCommandBuffer.
   - PolicyCommandBuffer uses std::array<PolicyCommand, 3> and a size counter.
   - evaluate() no longer calls reserve(), push_back(), or heap-allocating vector construction.
   - main.cpp now iterates over PolicyCommandBuffer.

2. Removed the same hot-path std::vector<PolicyCommand> allocation from AppliedPolicyTracker::evaluate().
   - This function is also called inside the watchdog loop before LatencyDecisionLayer::evaluate().
   - It now returns PolicyCommandBuffer as well.

3. Removed std::string creation from LatencyMetricsCollector IPv4 formatting.
   - formatIpv4Address() now returns std::array<char, INET_ADDRSTRLEN>.
   - Logger call sites pass fixed-buffer .data().
   - The ICMP sensor path no longer constructs std::string just to format IPv4 addresses.

Validation notes:
- No remaining std::vector<PolicyCommand> usage found.
- No remaining std::string-returning formatIpv4Address() usage found.
- Windows-specific build was not executed in this Linux container.
